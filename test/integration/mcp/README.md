# MCP Integration Tests

This directory is reserved for end-to-end MCP integration tests.

Planned coverage:
- recipe synthesis against real Spring fixtures
- strict line-probe verification
- Java-agent runtime instrumentation checks
- raw stdio transport conformance (stdout protocol-only, stderr diagnostics)
