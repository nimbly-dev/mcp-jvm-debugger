# Java Agent Integration Tests

This directory is reserved for direct Java-agent integration tests.

Planned coverage:
- launching fixture Spring applications with `-javaagent`
- verifying probe runtime health and line resolution over HTTP
- asserting hit-count, capture, and actuation behavior against real instrumented code
